{
  "name": "dev-tools",
  "private": true,
  "scripts": {
    "dev:harness": "pnpm --filter harness dev",
    "build:harness": "pnpm --filter harness build"
  },
  "engines": {
    "node": ">=18",
    "pnpm": ">=9"
  }
}
