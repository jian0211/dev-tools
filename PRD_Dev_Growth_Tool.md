# PRD: Dev Growth Tool

> **버전**: 0.2
> **작성일**: 2026-04-01
> **상태**: 검토 전
> **저장 방식**: Markdown (로컬)
> **사용 범위**: 개인용

---

## 1. 개요 (Overview)

개발자가 작업 흐름 속에서 **스스로 성장**하고, 동시에 **AI도 함께 성장**할 수 있도록 설계된 개인용 개발 보조 툴.

4단계 파이프라인(Context → Plan → Code → Review)을 통해 모든 작업을 의식적으로 진행하고, 각 단계에서 셀프 리뷰를 통해 지식 공백을 식별·해소하는 사이클을 만든다.

---

## 2. 배경 및 문제 정의

| 문제 | 설명 |
|------|------|
| 반복 없는 개발 | 코드를 쓰고 넘어가면 무엇을 배웠는지 남지 않는다 |
| 지식 공백 인식 부재 | 모르는 것을 모른 채로 진행하는 경우가 많다 |
| 컨텍스트 휘발 | 회의록, 결정 배경이 기록되지 않아 나중에 왜 이렇게 했는지 알 수 없다 |
| AI와의 단방향 관계 | AI는 답을 줄 뿐, 나와 함께 성장하지 않는다 |
| 성장 추적 불가 | 지난 나와 지금 나를 비교할 수 있는 데이터가 없다 |

---

## 3. 목표 (Goals)

1. **컨텍스트 보존**: 작업의 배경과 문제 정의를 구조화된 문서로 남겨 언제든 돌아볼 수 있게 한다.
2. **의식적 개발**: 계획 → 구현 → 검증의 흐름을 명확히 하고, 무의식적 코딩을 방지한다.
3. **개발자 성장**: 각 단계에서 셀프 리뷰를 통해 이해도와 지식 공백을 파악한다.
4. **AI 성장**: 개발자의 패턴, 선호, 지식 수준을 AI가 누적 학습하여 점점 더 맞춤화된 조력자가 된다.

---

## 4. 핵심 파이프라인 (4-Stage Pipeline)

모든 작업(Feature/Task)은 아래 4단계를 순서대로 거친다.

```
┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐
│ Stage 1  │───▶│ Stage 2  │───▶│ Stage 3  │───▶│ Stage 4  │
│ Context  │    │  Plan    │    │  Code    │    │  Review  │
└──────────┘    └──────────┘    └──────────┘    └──────────┘
```

> **의존성 원칙**: 각 단계는 이전 단계에 강하게 의존한다.
> 상위 단계가 수정되면, 이후 모든 단계의 산출물은 **재검토 상태**로 전환된다.

---

### Stage 1 — Context 문서 작성

**목적**: 처음 보는 사람도 이 문서 하나만 읽으면 배경과 문제를 이해할 수 있도록 정리한다.

**입력**:
- 회의록, 로그, 메모, 슬랙 스레드 등 날 것의 기록

**출력**: Context 문서 (CPR 형식 — *정확한 명칭 추후 확인*)

| 섹션 | 내용 |
|------|------|
| **Context** | 이 작업이 왜 필요한가? 어떤 배경에서 시작됐는가? |
| **Problem** | 해결해야 할 문제는 무엇인가? 현재 무엇이 안 되고 있는가? |
| **Resolution** | 어떤 방향으로 해결할 것인가? (아직 "어떻게"가 아닌 "무엇을") |

**프로세스**:
1. 날 것의 로그/메모를 툴에 붙여넣기
2. AI가 CPR 구조로 초안 작성
3. 사용자가 검토 및 확정
4. 확정된 Context 문서를 기반으로 Stage 2 진입 가능

---

### Stage 2 — Plan 작성

**목적**: Context 문서를 기반으로 "어떻게 만들 것인가"를 구체화한다.

**입력**: 확정된 Context 문서

**출력**: Plan 문서

| 섹션 | 내용 |
|------|------|
| **Architecture** | 전체 구조, 기술 스택, 데이터 흐름 |
| **Task Breakdown** | 작업을 독립적인 단위로 분할 |
| **Parallel Map** | 어떤 Task가 병렬 진행 가능한지 의존성 매핑 |
| **Harness Setup** | 개발 환경, 테스트 구조, 검증 기준 정의 |
| **Risk** | 예상되는 기술적 어려움 및 불확실성 |

**프로세스**:
1. AI가 Context 문서를 분석하여 Plan 초안 제안
2. Task를 병렬 가능 여부에 따라 분류
3. 사용자가 검토 및 확정
4. 확정된 Plan을 기반으로 Stage 3 진입 가능

---

### Stage 3 — Code (구현 + 검증)

**목적**: Plan을 기반으로 실제 구현을 진행하고, 각 Task 완료 후 검증한다.

**입력**: 확정된 Plan 문서

**프로세스**:
```
[Task 시작]
    │
    ├─ 병렬 가능한 Task → AI 에이전트가 동시 진행
    │
    ▼
[구현 (Write)]
    │
    ▼
[검증 (Verify)]
    ├─ 자동: 테스트 실행, 린트, 타입 체크
    └─ 수동: 하네스 체크리스트 확인
    │
    ▼
[Task 완료 → 미니 셀프 리뷰]
    ├─ 이해한 것
    ├─ 모르는 것 → Knowledge Backlog 등록
    └─ 다음 Task에 영향 여부 확인
```

**산출물**:
- 구현된 코드
- Task별 미니 셀프 리뷰 기록
- Knowledge Backlog 항목 (모르는 것)

---

### Stage 4 — Self Review & PR

**목적**: 전체 작업을 나 자신이 검토하고, 문제 없음을 확인하면 PR을 생성한다.

**입력**: Stage 3의 구현 결과 + 모든 미니 리뷰 기록

**프로세스**:
1. AI가 Stage 3 전체 요약 및 변경 사항 정리
2. 사용자가 셀프 리뷰 수행:
   - 의도대로 구현되었는가?
   - 놓친 케이스는 없는가?
   - Context 문서의 문제를 실제로 해결했는가?
3. 이슈 발견 시 → 해당 Stage로 되돌아가 재작업
4. **확인(Approve)** → PR 자동 생성

**PR 자동 포함 내용**:
- Context 문서 요약 (배경 & 문제)
- Plan 요약 (아키텍처 & 접근 방식)
- 변경 사항 목록
- 셀프 리뷰 체크리스트 결과

---

## 5. 단계 간 의존성 & 재검토 규칙

```
Stage 1 수정 → Stage 2, 3, 4 전부 재검토 필요
Stage 2 수정 → Stage 3, 4 전부 재검토 필요
Stage 3 수정 → Stage 4 재검토 필요
```

**재검토 트리거 조건**:
- 문제 정의가 바뀜
- 아키텍처/기술 스택 변경
- 구현 중 Plan과 다른 방향으로 진행됨
- 셀프 리뷰에서 근본적인 문제 발견

재검토가 트리거되면 툴은 영향받는 하위 단계 문서를 `⚠️ 재검토 필요` 상태로 표시한다.

---

## 6. Knowledge Gap Loop

셀프 리뷰(Stage 3 미니 리뷰 / Stage 4 최종 리뷰)에서 발견된 `모르는 것`은 Knowledge Backlog로 관리된다.

```
[모르는 것 발견]
      │
      ▼
[Knowledge Backlog 등록]
  - 제목
  - 발견된 Stage/Task
  - 관련 컨텍스트
      │
      ▼
[학습]
      │
      ▼
[재확인 리뷰] → 해소됨? → [완료 & 성장 기록]
                         → 아직? → [Backlog 유지]
```

---

## 7. AI 성장 모델

AI는 개발자의 누적 데이터를 기반으로 점점 더 맞춤화된 조력자가 된다.

| 데이터 | AI 활용 방식 |
|--------|------------|
| 미니 리뷰의 `모르는 것` 패턴 | 반복되는 공백 영역 선제적 경고 |
| Stage별 소요 시간 | 병목 Stage 파악 및 Harness 개선 제안 |
| 과거 Context 문서들 | 유사한 문제 발생 시 이전 해결 방식 참조 |
| Knowledge Backlog 해소 이력 | 학습 성장 곡선 트래킹 |

---

## 8. 데이터 구조 (Markdown 기반)

```
.dev-growth/
├── tasks/
│   └── {task-id}/
│       ├── context.md       # Stage 1 산출물
│       ├── plan.md          # Stage 2 산출물
│       ├── reviews/
│       │   ├── task-{n}.md  # Stage 3 미니 리뷰
│       │   └── final.md     # Stage 4 셀프 리뷰
│       └── status.md        # 현재 Stage, 재검토 여부
├── knowledge-backlog/
│   └── {gap-id}.md          # Knowledge Gap 항목
└── profile/
    └── growth.md            # 누적 성장 기록
```

---

## 9. 비기능 요구사항

| 항목 | 요구사항 |
|------|---------|
| 저장 방식 | Markdown, 로컬 파일 시스템 |
| 사용 범위 | 완전 개인용 (팀 공유 없음) |
| 마찰 최소화 | Stage 간 전환 및 리뷰 입력이 2분 이내 |
| AI 연동 | Claude API 기반 |
| 인터페이스 | CLI 우선, 선택적 웹 UI |
| 확장성 | VS Code 플러그인 연동 가능한 구조 |

---

## 10. 미결 사항 (Open Questions)

- [ ] CPR 문서 형식의 정확한 명칭 확인
- [ ] Stage 3에서 AI 병렬 작업의 구체적인 실행 방식 (서브에이전트? 독립 프로세스?)
- [ ] `재검토 필요` 상태일 때 이전 산출물을 덮어쓸지, 버전 관리할지
- [ ] PR 자동 생성 시 연동할 Git 호스팅 (GitHub only? GitLab?)
- [ ] Knowledge Backlog의 학습 방식을 툴 내에서 지원할지, 외부 링크만 연결할지

---

## 11. 성공 지표

- 전체 Stage 중 셀프 리뷰 완료율 Y% 이상
- Knowledge Gap 발견율: Task당 평균 N개
- Gap 해소율: Backlog 항목 중 X% 이상 재확인 완료
- `재검토 필요` 발생 빈도 감소 (파이프라인 안정화 지표)

---

## 12. 다음 단계 (Next Steps)

1. Open Questions 의사결정 (특히 CPR 형식, Stage 3 병렬 실행 방식)
2. MVP 범위 확정: Stage 1~4 기본 플로우 + Knowledge Backlog
3. 데이터 모델 상세 설계 (Markdown 스키마)
4. CLI 프로토타입 구현 시작
